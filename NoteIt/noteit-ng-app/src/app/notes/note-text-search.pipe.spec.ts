import { NoteTextSearchPipe } from './note-text-search.pipe';

describe('NoteTextSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteTextSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
